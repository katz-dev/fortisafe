<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background-color: #dc3545;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }

        .content {
            background-color: #f9f9f9;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 0 0 5px 5px;
        }

        .alert {
            background-color: #fff3cd;
            border: 1px solid #ffeeba;
            color: #856404;
            padding: 15px;
            margin: 15px 0;
            border-radius: 4px;
        }

        .details {
            background-color: #e9ecef;
            border: 1px solid #dee2e6;
            padding: 15px;
            margin: 15px 0;
            border-radius: 4px;
        }

        .details h3 {
            margin-top: 0;
            color: #495057;
        }

        .details p {
            margin: 5px 0;
        }

        .footer {
            text-align: center;
            margin-top: 20px;
            color: #666;
            font-size: 12px;
        }
    </style>
</head>

<body>
    <div class="header">
        <h1>Password Security Alert</h1>
    </div>
    <div class="content">
        <p>Hello {{name}},</p>

        <div class="alert">
            <p><strong>Security Alert:</strong> We've detected a potential security issue with your password.</p>
            {{#if isCompromised}}
            <p>Your password has been found in known data breaches. This means it's no longer secure and should be
                changed immediately.</p>
            {{/if}}
            {{#if isWeak}}
            <p>Your password is considered weak and may be easily guessed. We recommend using a stronger password.</p>
            {{/if}}
        </div>

        {{#if passwordDetails}}
        <div class="details">
            <h3>Affected Account Details:</h3>
            <p><strong>Website:</strong> {{passwordDetails.website}}</p>
            <p><strong>Username:</strong> {{passwordDetails.username}}</p>
            {{#if passwordDetails.url}}
            <p><strong>URL:</strong> {{passwordDetails.url}}</p>
            {{/if}}
            {{#if passwordDetails.breachCount}}
            <p><strong>Times Found in Data Breaches:</strong> {{passwordDetails.breachCount}}</p>
            {{/if}}
            {{#if passwordDetails.isUrlUnsafe}}
            <p><strong>URL Safety:</strong> <span style="color: #dc3545;">Unsafe</span></p>
            <p><strong>Threat Types:</strong> {{passwordDetails.urlThreatTypes}}</p>
            {{/if}}
            {{#if passwordDetails.isReused}}
            <p><strong>Password Reuse:</strong> <span style="color: #dc3545;">This password is reused across multiple
                    accounts</span></p>
            <p><strong>Reused In:</strong> {{passwordDetails.reusedIn}}</p>
            {{/if}}
        </div>
        {{/if}}

        <p>For your security, please:</p>
        <ul>
            <li>Change your password immediately</li>
            <li>Use a strong, unique password</li>
            <li>Enable two-factor authentication if available</li>
        </ul>

        <p>If you need help creating a strong password, consider using a password manager or following these guidelines:
        </p>
        <ul>
            <li>Use at least 12 characters</li>
            <li>Include uppercase and lowercase letters</li>
            <li>Include numbers and special characters</li>
            <li>Avoid using personal information</li>
        </ul>
    </div>
    <div class="footer">
        <p>This is an automated security alert from FortiSafe</p>
    </div>
</body>

</html>